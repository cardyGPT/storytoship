{
  "name": "elixir-labs-nested-tabs",
  "version": "1.0.0",
  "description": "Nested Tab Navigation System",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start:backend": "npm run start:dev --workspace=backend",
    "start:frontend": "npm run start --workspace=frontend",
    "docker:up": "docker-compose up --build"
  }
}
