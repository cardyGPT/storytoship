{
  "name": "cardinality-i18n-rbac",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && npm run start:dev",
    "start:frontend": "cd frontend && npm start",
    "docker:up": "docker-compose up --build"
  }
}